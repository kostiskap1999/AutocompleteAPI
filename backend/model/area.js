class Area {
  constructor({ place_id, main_text, secondary_text }) {
    this.placeId = place_id
    this.mainText = main_text
    this.secondaryText = secondary_text
  }
}

module.exports = { Area }
